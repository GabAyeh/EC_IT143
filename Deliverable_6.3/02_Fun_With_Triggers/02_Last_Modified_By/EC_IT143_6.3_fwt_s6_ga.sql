USE [EC_IT143_DA];
GO

-- Both required trigger values are complete.
-- Next question: How could I store a permanent history of every change in a separate audit table?

SELECT
    'How could I store a permanent history of every change in a separate audit table?' AS next_question,
    'A separate audit table could preserve old and new values instead of only the latest change.' AS starting_idea;
GO

